{
  "name": "@lbptracker/web",
  "version": "0.2.9",
  "private": true,
  "type": "module",
  "description": "The tracker's pages: the instrument bench, the live player, the offline renderer and the MIDI bridge.",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "stage": "vite build && node dev/stage-site.ts",
    "deploy": "npm run stage && wrangler deploy",
    "test": "node --test",
    "typecheck": "node dev/typecheck.mjs"
  },
  "dependencies": {
    "@lbptracker/cwlib": "*",
    "@lbptracker/lib": "*",
    "vue": "^3.5.42"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.8",
    "vite": "^8.2.2",
    "vue-tsc": "^3.3.11",
    "wrangler": "^4.129.0"
  }
}
